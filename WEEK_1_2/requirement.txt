# requirements.txt
# This file requires Python 3.11 or higher.

# Core dependencies (examples):
requests>=2.31.0
python-dotenv>=1.0.0
regex>=2023.10.3

# If you need a CSV/Excel library, you can add it here:
# pandas>=2.1.1

# If you plan to run tests, consider adding pytest or other testing libraries:
# pytest>=7.4.2
